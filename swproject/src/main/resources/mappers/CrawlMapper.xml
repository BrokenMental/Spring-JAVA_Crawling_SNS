<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.swproject.mapper.CrawlMapper">
	<insert id="create">
		insert into Crawler (N_Title, URL)
		values(#{N_Title}, #{URL})
	</insert>
</mapper>